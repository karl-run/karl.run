<div class="content">
  <slot />
</div>

<style>
  .content :global(h1) {
    margin-bottom: 1rem;
    font-size: 2.25rem;
    line-height: 2.5rem;
    font-weight: 700;
  }

  .content :global(h2) {
    margin-bottom: 0.25rem;
    font-size: 1.5rem;
    line-height: 2rem;
    font-weight: 700;
  }

  .content :global(h3) {
    margin-bottom: 0.25rem;
    font-size: 1.25rem;
    line-height: 1.75rem;
    font-weight: 700;
  }

  .content :global(h4) {
    margin-bottom: 0.25rem;
    font-size: 1.125rem;
    line-height: 1.75rem;
    font-weight: 700;
  }

  .content :global(h5) {
    margin-bottom: 0.25rem;
    font-size: 1rem;
    line-height: 1.5rem;
    font-weight: 700;
  }

  .content :global(h6) {
    margin-bottom: 0.25rem;
    font-size: 0.875rem;
    line-height: 1.25rem;
    font-weight: 700;
  }

  .content :global(p),
  .content :global(ul),
  .content :global(ol),
  .content :global(blockquote),
  .content :global(pre),
  .content :global(hr),
  .content :global(img) {
    margin-bottom: 1rem;
  }

  .content :global(ul) {
    list-style: disc;
    list-style-position: inside;
  }

  .content :global(ol) {
    list-style: decimal;
    list-style-position: inside;
  }

  .content :global(li) {
    margin-bottom: 0.5rem;
  }

  .content :global(a) {
    color: #0d9e91;
  }

  .content :global(a:hover) {
    text-decoration: underline;
  }

  .content :global(blockquote) {
    position: relative;
    overflow: hidden;
    border-left: 0.35rem solid #0d9e91;
    border-radius: 0.75rem;
    background: linear-gradient(135deg, rgb(255 251 235 / 95%), rgb(240 249 255 / 92%));
    color: #1f2937;
    padding: 1.25rem 1.25rem 1.25rem 1.5rem;
    box-shadow:
      0 10px 30px rgb(13 158 145 / 10%),
      inset 0 1px 0 rgb(255 255 255 / 45%);
  }

  .content :global(blockquote)::before {
    content: '“';
    position: absolute;
    right: 0.75rem;
    top: -0.6rem;
    font-size: 4.5rem;
    line-height: 1;
    color: rgb(13 158 145 / 18%);
    font-family: Georgia, serif;
    pointer-events: none;
  }

  .content :global(blockquote > p) {
    margin: 0;
    font-size: 1.05rem;
    line-height: 1.8;
    font-style: italic;
  }

  .content :global(blockquote > :last-child) {
    margin-bottom: 0;
  }

  .content :global(hr) {
    margin-top: 1rem;
    margin-bottom: 1rem;
  }

  .content :global(pre) {
    margin-bottom: 1rem;
    padding: 0.5rem;
  }

  .content :global(pre code) {
    background-color: transparent;
    padding: 0;
  }

  .content :global(code) {
    background-color: #2d2b57;
    padding: 0.125rem;
  }

  :global(.dark) .content :global(blockquote) {
    background: linear-gradient(135deg, rgb(30 41 59 / 96%), rgb(15 23 42 / 94%));
    color: rgb(226 232 240);
    box-shadow:
      0 10px 30px rgb(2 6 23 / 45%),
      inset 0 1px 0 rgb(255 255 255 / 5%);
  }

  :global(.dark) .content :global(blockquote)::before {
    color: rgb(45 212 191 / 14%);
  }
</style>
